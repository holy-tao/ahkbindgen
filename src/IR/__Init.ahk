/************************************************************************
 * IR types - these are extracted via libclang and then transformed as
 * type resolution, method attribution, and so forth are done. Types extend
 * Map so they inherit extensions
 ***********************************************************************/

#Import export Struct {*}
#Import export Type {*}
